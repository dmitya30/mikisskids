# Текущий статус MIKISSKIDS

**Актуализировано:** 03.08.2026
**Назначение:** главный файл передачи контекста между рабочими сессиями.

> Перед новым блоком читать этот файл, затем `DECISIONS.md`, `TODO.md`,
> последние записи `OPERATIONS.md` и последние коммиты Git.

## Сводка на 03.08.2026

Этот раздел имеет приоритет над более ранними статусами ниже, если между
ними обнаружено противоречие. Исторические сведения сохранены намеренно.

### Завершённые операции

- GitHub `main` перед документационным обновлением:
  `6621b524bb45172c6c9220351fcb8f49080f725e`.
- Упоминания Ozon и Wildberries удалены из сайта и документации.
- Дата поступления чемодана изменена на 31.08.2026 и синхронизирована
  с основным сайтом и Tilda.
- Возвраты двух контрольных оплат по 1 ₽ подтверждены фактическим
  поступлением денег через один день.
- Ошибка CloudKassir относится к настройкам онлайн-кассы и вынесена
  в отдельную работу с доступами владельца и технической поддержки.
- Ошибка webhook HTTP 404 устранена восстановлением канонического
  Nginx include.
- `tilda-webhook.service` активен.
- `nginx -t` проходит.
- Production webhook route возвращает HTTP 200.
- Тестовый webhook-запрос не создаёт заказ.
- Оплаченный заказ Tilda `1398406812` на 50 000 ₽ вручную восстановлен
  в SQLite со статусом `paid_reported_by_tilda`.
- Дублей нет; ожидающая оплаты заявка не импортирована.
- Backup SQLite перед восстановлением создан.

### Принятые ограничения

- SQLite является внутренней резервной копией заявок и не влияет
  на оплату, выручку, бухгалтерскую или налоговую отчётность.
- Google Search Console не подключается.
- Дальнейшее внедрение и ужесточение DMARC не выполняется.
- CloudKassir не исправляется без отдельного согласования и доступов.
- Исторический ошибочный webhook нельзя повторно отправить из Tilda.
- Core Web Vitals остаётся отдельной незавершённой проверкой.

### Следующий этап

Запрошены российский каталог и международная версия для EU и USA.

Российский контур сохраняется:

```text
mikisskids.ru на VPS
    → pay.mikisskids.ru на Tilda
    → Т-Банк
    → Tilda webhook
    → SQLite на VPS
```

Международная архитектура ещё не выбрана. Исследуются:

1. storefront на VPS и отдельный hosted checkout;
2. storefront на VPS и Shopify как commerce backend/checkout;
3. полный международный storefront в Shopify.

Полный переход международного storefront на Shopify не утверждён.
Окончательный план, срок и цена принимаются после discovery и
архитектурного сравнения.

Сопутствующие драфты:

- `ROADMAP_V2.md`;
- `INTERNATIONAL_ARCHITECTURE_RESEARCH.md`;
- `CATALOG_REQUIREMENTS.md`;
- `DISCOVERY_QUESTIONS.md`;
- `COMPLIANCE_MATRIX.md`;
- `ESTIMATE_V2.md`.

## Репозиторий и production

- Репозиторий: `dmitya30/mikisskids`.
- Ветка: `main`.
- Текущая база GitHub перед документацией V2:
  `6621b52` - удаление упоминаний Ozon и Wildberries.
- Открытие индексации: `a76476c`.
- Исправление webhook без `tranid`: `83d337f`.
- Каноническая Nginx-конфигурация и webhook route из `6b7dccd`
  развёрнуты и проверены в production.
- Публичный контент после `6621b52` проверен на основном сайте.
- Техническое SEO, оптимизация медиа и единый механизм согласия
  развёрнуты и проверены.
- Пользователь сайта на VPS: `mikiss`.
- Репозиторий на VPS:
  `/var/www/mikiss/data/repos/mikisskids`.
- Production webroot:
  `/var/www/mikiss/data/www/mikisskids.ru`.
- Команда статического деплоя:
  `sudo /usr/local/sbin/deploy-mikisskids`.

## Основной сайт

Основной сайт работает на VPS:

- `https://mikisskids.ru/` возвращает HTTP 200;
- HTTP перенаправляется на HTTPS;
- `www.mikisskids.ru` перенаправляется на основной домен;
- сертификат Let's Encrypt действует для основного домена и `www`;
- конфигурация Nginx проходит `nginx -t`;
- приватные каталоги backend, deploy, docs и scripts не публикуются;
- пять исторических URL прежнего Tilda-сайта перенаправляются кодом 301
  на актуальные страницы;
- старая `/success_payment` возвращает 410 и не перенаправляется
  на действующую страницу успеха.

Функциональные изменения:

- все CTA «Оформить заказ» направляют на `pay.mikisskids.ru`;
- товарные CTA добавляют `?product=chair` или `?product=travel-seat`;
- переходы проверены на desktop и mobile;
- устаревший `<order-dialog>`, его JavaScript и CSS удалены;
- якоря `/chair/#details` и `/travel-seat/#transform` исправлены;
- consent-модуль подключён ко всем 10 страницам основного сайта;
- аудит внутренних ссылок обрабатывает 11 HTML-файлов, включая
  deployment-фрагмент Tilda, и 91 локальную ссылку/ресурс;
- FAQ двух товаров полностью сохранены: 6 вопросов для стульчика
  и 7 вопросов для чемодана;
- условия наличия, предзаказа и доставки показаны на товарных страницах,
  странице доставки, в checkout Tilda и публичной оферте;
- стульчик находится в наличии и передаётся в доставку в течение
  одного рабочего дня после оплаты;
- чемодан доступен по предзаказу с ожидаемым поступлением
  до 31.08.2026 и передачей в доставку в течение одного рабочего дня;
- доставка включена в стоимость: Москва и Московская область -
  курьером до адреса, остальные регионы - до согласованного ПВЗ.

## DNS и почта

Домен обслуживается FASTVPS DNS:

- основной домен направлен на VPS;
- `pay.mikisskids.ru` направлен на Tilda;
- MX, SPF и DKIM Яндекса сохранены.

Почта `welcome@mikisskids.ru` проверена:

- входящая почта — OK;
- исходящая почта — OK;
- SPF — PASS;
- DKIM — PASS;
- DMARC опубликован в режиме наблюдения `p=none`.

До накопления отчётов DMARC не переводить политику в
`quarantine` или `reject`.

## Платёжный контур

Используется схема:

```text
основной сайт на VPS
        ↓
pay.mikisskids.ru на Tilda
        ↓
корзина ST100
        ↓
готовый модуль Tilda → Т-Банк
        ↓
Tilda webhook после оплаты
        ↓
SQLite на VPS
```

В Tilda опубликованы только две страницы:

- `payment-mvp` - главная платёжная страница, доступная по
  `https://pay.mikisskids.ru/`;
- `payment-success` - единственная страница успешной оплаты:
  `https://pay.mikisskids.ru/payment-success`.

Старая `success_payment` переименована в `success_payment_old`
и снята с публикации. Остальные устаревшие страницы Tilda также
сняты с публикации.

На Tilda:

- используются два основных товара;
- используется корзина ST100;
- подключён готовый модуль Tilda → Т-Банк;
- webhook отправляется только после оплаты;
- cookies и JSON в webhook отключены;
- API METHOD оставлен `Off`;
- общий consent-модуль подключён через HEAD-код проекта;
- встроенные интеграции Яндекс Метрики и Mail.ru/VK отключены;
- legacy-счётчики `107009480` и `3752851` удалены;
- единственная опубликованная success-страница имеет
  универсальный текст и `noindex, nofollow`;
- URL-цель `/payment-success` используется для оценки конверсии;
- серверное событие Метрики на текущем этапе не внедряется;
- источниками истины по оплате остаются webhook, Т-Банк и касса.

По информации владельца:

- терминал рабочий и активный;
- используется одностадийная оплата;
- подключён CloudKassir;
- применяется УСН «Доходы» 6%;
- заявлена ставка НДС 5%.

Работоспособность оплаты и полного возврата 10 ₽ подтверждена.
Налоговые параметры, реквизиты кассового чека и чеки ожидающих
возвратов по 1 ₽ необходимо подтвердить с владельцем. Доступ к
Т-Бизнесу остаётся только у владельца проекта.

## Webhook на VPS

Минимальный приёмник Tilda Webhook развёрнут и работает:

- секретный HTTPS endpoint;
- только POST;
- приложение слушает `127.0.0.1:8091`;
- systemd unit `tilda-webhook.service`;
- SQLite: `/var/lib/mikisskids/orders.sqlite3`;
- уникальность и идемпотентность по `tranid`;
- чувствительные поля не сохраняются;
- резервное копирование включено;
- webhook URL не записывается в журналы;
- `test=test` возвращает 200 и не создаёт запись;
- повторный `tranid` не создаёт дубликат;
- все 8 unit-тестов прошли;
- реальная оплаченная заявка сохранена в SQLite;
- Tilda подтвердила успешную отправку webhook;
- полный возврат контрольной оплаты 10 ₽ подтверждён.

## Техническое SEO

В production развёрнуто:

- canonical для 5 основных страниц;
- Open Graph и Twitter Card для 5 основных страниц;
- `sitemap.xml` с 5 индексируемыми URL;
- ссылка на sitemap в `robots.txt`;
- `noindex, follow` и canonical для 4 legal-страниц;
- Organization и WebSite JSON-LD на главной;
- Product и Offer JSON-LD на страницах двух товаров;
- JSON-LD проверен через Google Rich Results Test в режиме кода,
  Schema.org Validator и валидатор Яндекса;
- внешние валидаторы не обнаружили ошибок;
- собственная страница 404;
- Nginx возвращает кастомную страницу с HTTP 404;
- прямой запрос `/404.html` также возвращает HTTP 404;
- всем HTML-изображениям и видео добавлены реальные `width` и `height`;
- изображения, создаваемые через `js/main.js`, также получили размеры;
- три hero-изображения используют `fetchpriority="high"` без lazy loading;
- lazy loading сохранён для изображений ниже первого экрана;
- автоматический SEO-аудит проверяет размеры медиа и приоритет hero;
- оба товарных MP4 переведены в Fast Start без перекодирования;
- production-видео поддерживают HTTP Range с кодом 206.

Индексация открыта коммитом `a76476c` после финального аудита.

Production `robots.txt` разрешает обход сайта и содержит ссылку на
Sitemap. В Яндекс Вебмастере Sitemap имеет статус `OK`, содержит пять
URL; все пять публичных страниц поставлены в очередь на переобход.

Проверка Core Web Vitals остаётся отдельной задачей после накопления
полевых данных.

Конвертация текущих JPEG в WebP/AVIF не является приоритетной:
самое крупное изображение занимает около 245,5 KiB. Основной тяжёлый
медиаресурс сайта — видео; для обоих MP4 уже включён Fast Start без
изменения сжатых медиапотоков.

## Аналитика

Счётчик Яндекс Метрики `111056887` установлен:

- на основном сайте `mikisskids.ru`;
- на платёжном поддомене `pay.mikisskids.ru`;
- только через единый consent-модуль;
- без Вебвизора;
- с сохранением выбора в cookie `mk_cookie_consent`
  для домена `.mikisskids.ru`.

Проверены сценарии:

- до согласия код Метрики не загружается;
- отказ сохраняется и не блокирует оформление заказа;
- выбор действует при переходах между основным доменом и `pay`;
- после согласия загружается только счётчик `111056887`;
- пользователь может изменить решение через «Настройки cookies».

С Tilda удалены ранее установленные legacy-счётчики:

- Яндекс Метрика `107009480`;
- Top.Mail.Ru / VK Pixel `3752851`.

Опубликована единственная страница возврата после оплаты:
`/payment-success`. Старая `success_payment` и остальные устаревшие
страницы Tilda сняты с публикации.

URL-цель `/payment-success` используется для оценки текущей конверсии.
Контрольный платёж подтвердил фактический Return URL
`https://pay.mikisskids.ru/payment-success`.

Открытие страницы успеха не является финансовым подтверждением оплаты.
Источниками истины остаются Т-Бизнес, Tilda, серверный webhook и
онлайн-касса. Серверное событие Метрики на текущем этапе решено
не внедрять.

Цели начала оформления, перехода в checkout, кликов по контактам и
открытия `/payment-success` проверены через отладчик Метрики.

## Проверка платёжного цикла 29.07.2026

Фактически проверены:

- реальная оплата через Tilda и Т-Банк;
- успешное завершение оплаты;
- Return URL `https://pay.mikisskids.ru/payment-success`;
- отображение оплаченной заявки в Tilda;
- полный возврат контрольной оплаты 10 ₽;
- отправка оплаченной заявки из Tilda в webhook;
- сохранение заказа в SQLite;
- восемь unit-тестов webhook;
- production deploy коммита `83d337f`.

Первоначально оплаченный webhook возвращал HTTP 400. Безопасное
диагностическое логирование подтвердило причину: production payload
Tilda не содержал `tranid`.

Реализован следующий приоритет стабильного идентификатора:

1. `tranid`;
2. хеш идентификатора заказа Tilda;
3. хеш `paymentid`.

Payload без стабильного идентификатора по-прежнему отклоняется.
После исправления новая оплаченная заявка успешно сохранена в SQLite
со статусом `paid_reported_by_tilda`, а Tilda показала успешную отправку
в Webhook.

Временные платёжные суммы 10 ₽ и 1 ₽ удалены. Платёжная сумма снова
составляет 50 000 ₽. Проверка двух возвратов по 1 ₽ и соответствующих
чеков передана владельцу, имеющему доступ к Т-Бизнесу.

Источник суммы в текущем MVP - товарная ссылка Tilda. Backend не создаёт
платёж и не сверяет сумму с серверным каталогом. Кабинет Т-Бизнеса и
онлайн-касса остаются источниками истины по оплате, возврату и чекам.

## Яндекс Вебмастер

- Сайт `https://mikisskids.ru/` добавлен в Яндекс Вебмастер.
- Права владельца подтверждены через DNS TXT.
- В Вебмастере сохранена история прежней Tilda-версии сайта:
  подключение от 07.07.2026 и последний обход от 21.07.2026.
- Исторические данные относятся к состоянию сайта до текущей разработки.
- Sitemap содержит пять публичных страниц основного сайта.
- Финальный SEO-, links-, consent- и webhook-аудит пройден.
- Коммитом `a76476c` удалён общий запрет `Disallow: /`.
- Production `robots.txt` разрешает обход сайта.
- Sitemap в Яндекс Вебмастере имеет статус `OK` и содержит 5 URL.
- Все пять публичных страниц отправлены на переобход и находятся
  в очереди.

## Следующий блок

1. Дождаться переобхода пяти публичных страниц и проверить их
   появление в поиске.
2. Получить подтверждение двух возвратов по 1 ₽ и чеков возврата.
3. Подтвердить кассовый чек и налоговые реквизиты.
4. Проверить Core Web Vitals после накопления полевых данных.
5. Проверить поступающие DMARC-отчёты.
6. Проверить обработку исторических 301/410 после переобхода.
