# Операционный журнал MIKISSKIDS

Этот файл ведётся как append-only журнал важных действий.

Правило работы:

1. Перед новым блоком обновить `STATUS.md`.
2. После изменения архитектуры обновить `DECISIONS.md`.
3. После завершения блока обновить `TODO.md`.
4. После production-действия добавить запись ниже.
5. Не записывать секреты, пароли, токены и персональные данные покупателей.

## 27.07.2026

- FAQ добавлены на страницы двух товаров.
- Страница бренда дополнена историей и фотографией основательницы.
- Исправлено мобильное меню.
- Опубликованы юридические документы.
- Email заменён на `welcome@mikisskids.ru`.
- На Tilda опубликована payment MVP страница.
- Проверен переход от двух товаров к платёжной странице Т-Банка.
- Принято решение оставить платёжный контур на Tilda.
- Принято решение хранить копии оплаченных заявок в SQLite на VPS.
- Деплой на VPS и перенос домена ещё не выполнялись.


- Подготовлен минимальный Tilda Webhook receiver.
- Добавлена SQLite-схема с уникальным `tranid`.
- Добавлены тесты идемпотентности и очистки чувствительных полей.
- Подготовлены systemd, Nginx и резервное копирование.
- Backend ещё не развёрнут на VPS и не подключён к Tilda.
- Выполнен read-only аудит VPS и FASTPANEL.
- Конфигурация адаптирована под пользователя `mikiss` и фактические пути сервера.
- Deploy-скрипт дополнен исключением приватных backend-каталогов из webroot.
- При первой серверной проверке backup обнаружены ограничения SQLite WAL и рабочего каталога.
- Backup timer отключён до установки исправления.
- Исправление backup установлено на VPS.
- Backup и восстановление SQLite успешно проверены.
- Webhook backend развёрнут и проверен на VPS.
- Nginx route установлен через FASTPANEL include.
- После попадания первого тестового URL в лог секрет был заменён.
- Логи очищены; повторная проверка подтвердила отсутствие webhook URL.
- Сервисы webhook, Nginx и backup timer работают.
- Tilda ещё не подключена; ожидается HTTPS после переноса домена.
- Локальный HTTP-тест webhook завершён успешно.
- Повторный `tranid` вернул HTTP 200 и не создал вторую запись.

- Статический сайт развёрнут в production-webroot VPS.
- Развёрнут коммит `a517b93`.
- Через Nginx получен `HTTP 200`.
- Подтверждено отсутствие приватных каталогов в webroot.
- DNS основного домена пока не переключался.


- Делегация `mikisskids.ru` перенесена на FASTVPS DNS.
- Основной домен направлен на VPS `5.183.190.117`.
- MX, SPF и DKIM Яндекса сохранены.
- Выпущен SSL для `mikisskids.ru` и `www.mikisskids.ru`.
- Настроены HTTP → HTTPS и `www` → основной домен.
- `pay.mikisskids.ru` подключён к Tilda и направлен на `176.57.66.234`.
- Для платёжного поддомена выпущен SSL.
- Webhook подключён к корзине ST100.
- Установлен коммит `0e8d89a`.
- Проверочный запрос Tilda `test=test` вернул `200 OK` и не был
  сохранён в SQLite.
- На VPS успешно выполнены все 6 unit-тестов webhook.
- Владельцам отправлен запрос по терминалу, кассе, ОФД, НДС и возврату.

## 27.07.2026 — функционал, почта и начало SEO

- Все CTA основного сайта переведены на `pay.mikisskids.ru`.
- Для товарных страниц добавлена передача параметра намерения покупателя.
- Проверены desktop- и mobile-переходы к оформлению.
- Исправлены якоря подробностей на страницах двух товаров.
- Удалён неработающий прототип заказа из HTML и JavaScript.
- Добавлен автоматический аудит внутренних ссылок.
- Удалены неиспользуемые стили прототипного диалога.
- В production последовательно развёрнуты коммиты до `cf54189`.
- Проверены входящая и исходящая почта.
- SPF и DKIM прошли проверку.
- Добавлена DMARC-запись с политикой наблюдения `p=none`.
- От владельца получены сведения о рабочем одностадийном терминале,
  CloudKassir и налоговых параметрах; полный платёжный цикл пока
  не проверен.
- Владелец самостоятельно ведёт настройку платёжного контура.
- Получен ID счётчика Яндекс.Метрики `111056887`; счётчик ещё
  не установлен.
- В коммитах `a406d30` и `c5c5911` подготовлены canonical,
  Open Graph, sitemap и метаданные legal-страниц.
- SEO-коммиты ещё не развёрнуты.
- Индексация остаётся закрытой до окончания технического аудита.

## 27.07.2026 — деплой технического SEO

- В production развёрнут коммит `75b25f4`.
- Опубликованы canonical, Open Graph и Twitter Card.
- Опубликован `sitemap.xml` с пятью индексируемыми URL.
- Legal-страницы получили `noindex, follow` и canonical.
- На главной опубликована JSON-LD-разметка Organization и WebSite.
- На страницах двух товаров опубликованы Product и Offer.
- Проверены цены `35000 RUB` и `50000 RUB` в структурированных данных.
- Создана собственная страница 404.
- В Nginx добавлен `error_page 404 /404.html`.
- Неизвестный URL возвращает HTTP 404 и пользовательскую страницу.
- Прямой запрос `/404.html` возвращает HTTP 404.
- Конфигурация Nginx прошла проверку и была перезагружена.
- `robots.txt` продолжает блокировать индексацию через `Disallow: /`.
- Основные страницы, sitemap, canonical и JSON-LD проверены в production.

## Шаблон следующей записи

```text
## ДД.ММ.ГГГГ

- Что изменено.
- Где развёрнуто.
- Как проверено.
- Какой коммит.
- Что осталось.
```
